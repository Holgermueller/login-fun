<li>
    {{burger_name}}
    <button class="devoured" data-id="{{id}}" data-nowDevoured="{{devoured}}" aria-disabled="disabled">
        {{#if devoured}}YUM!!{{else}}EAT IT!!{{/if}}
    </button>
</li>